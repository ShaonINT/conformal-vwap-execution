# Uncertainty-Aware Execution — pinned dependencies (Python 3.10/3.11).
# Verified mutually compatible.
#
#   python -m venv .venv && source .venv/bin/activate
#   pip install -r requirements.txt

# --- scientific core ---
numpy==1.24.4
pandas==1.5.3
scipy==1.10.1
scikit-learn==1.3.2       # GradientBoostingRegressor for the return predictor
matplotlib==3.7.5

# --- RL agent (scripts/run_ppo.py) ---
gymnasium==0.29.1
stable-baselines3==2.3.2
torch==2.2.2              # CPU/MPS wheel on macOS

# --- real-data validation (optional; Section 6 of the paper) ---
yfinance
